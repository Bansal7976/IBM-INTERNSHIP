# ─── Core ───────────────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
PyYAML>=6.0
scipy>=1.11.0
tqdm>=4.66.0

# ─── 2D Detection (YOLOv11, RT-DETR, YOLOv10) ───────────────────────────────
ultralytics>=8.3.0          # includes YOLOv11, YOLOv10, RT-DETR, ByteTrack

# ─── Open-Vocabulary Detection (Grounding DINO) ──────────────────────────────
# groundingdino-py          # uncomment if using Grounding DINO
# huggingface-hub>=0.20.0

# ─── 3D Detection (BEVFormer, BEVFusion) ─────────────────────────────────────
# Install MMDetection3D separately (version matters):
#   pip install openmim
#   mim install mmengine mmcv>=2.0 mmdet>=3.0 mmdet3d>=1.3
#
# Or Docker: docker pull openmmlab/mmdetection3d:latest

# ─── Dataset Toolkits ────────────────────────────────────────────────────────
nuscenes-devkit>=1.1.10
# pyquaternion>=0.9.9       # required by nuscenes-devkit

# ─── Augmentation ────────────────────────────────────────────────────────────
albumentations>=1.3.0

# ─── Visualization ───────────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
tensorboard>=2.14.0

# ─── Export / Deployment ─────────────────────────────────────────────────────
onnx>=1.14.0
onnxruntime>=1.16.0          # CPU inference
# onnxruntime-gpu>=1.16.0    # GPU inference (use instead of onnxruntime)
onnxsim>=0.4.33              # ONNX simplification

# ─── Notebooks ───────────────────────────────────────────────────────────────
jupyter>=1.0.0
ipywidgets>=8.0.0
